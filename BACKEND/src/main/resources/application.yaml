spring:
  autoconfigure:
    exclude: org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration
  jpa:
    show-sql: true
    properties:
      format_sql: true
      dialect: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://i9c104.p.ssafy.io:43306/ssafy
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: keepham@mysql!@#$
  data:
    mongodb:
      url: mongodb://i9c104.p.ssafy.io:47017
      username: root
      password: root
#      authentication-database: admin
#      database: ssafy

  kafka:
    consumer:
      bootstrap-servers: localhost:19091,localhost:19093,localhost:19094
      group-id: chatting
    producer:
      bootstrap-servers: localhost:19091,localhost:19093,localhost:19094
    template:
      default-topic: kafka-chat

  security:
    user:
      name: ssafy
      password: ssafy



springdoc:
  packages-to-scan: com.ssafy.keepham
  default-consumes-media-type: application/json;charset=UTF-8
  default-produces-media-type: application/json;charset=UTF-8
  swagger-ui:
    path: keepham-ui.html
    tags-sorter: alpha
    operations-sorter: alpha
    api-docs:
      path: /api-docs/json
      groups:
        enabled: true
    cache:
      disabled: true



